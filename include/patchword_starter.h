#ifndef PATCHWORD_STARTER_H
#define PATCHWORD_STARTER_H

// Generate a starter substring of length 3 or 4.
// The substring is chosen based on frequency and playability scoring.
const char *generate_starter(int length);

#endif

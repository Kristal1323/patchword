#include "worker.h"
#include "protocol.h"
#include "util.h"

int worker_main(int argc, char **argv) {
    log_worker(0, "Worker startup (args=%d)", argc);

    // TODO: implement later
    return 0;
}